# Direction contract — Fabric / Form

Confirmed request: use Impeccable, Kailash Urja and the super-dynamic preset. User clarified the motion should be simple and restrained, not everywhere or over-engineered. Existing math, account behavior, owner route/password, and saved records are fixed.

The named presets pin the visual world and override the concept seed's assigned index 7 and old PRODUCT.md color restriction. Seed 488dc612 was run before code edits; its unrelated worlds are not adopted over the user's explicit references. Their useful craft disciplines: instrument clarity, material depth and strong hierarchy, without adopting meter, ASCII, paper, bazaar, mascot or VHS motifs.

FIRST VIEWPORT: full-width atmospheric dark teal (#062c2d), oversized Archivo headline left, a genuine interactive cloth GLB on the right under soft light, lime primary action, understated functional navigation. The object occupies a stage, not a card. Faint textile construction lines supply depth. No unrelated biogas claims. On narrow phones the headline precedes a compact cloth scene and a visible Open studio action. Login is a real destination below, reachable from the header.

Signature interaction: inspect the cloth with horizontal drag or left/right arrow controls; switch three illustrative material colors; pause movement. Gentle bounded idle rotation only in this scene, stopped offscreen/hidden/reduced-motion. WebGL or load failure keeps the illustrative fallback and all primary actions. Copy explicitly labels the scene a fabric study, not actual cost data.

Mode: entrance Persuade; calculator, results and owner ledger Operate. Shared Archivo, teal, lime, sage and clean dividers tie all surfaces together. Calculator controls remain fully stable. Transition motion stays under 300ms, reduced-motion removes spatial movement.

Cross-surface: pale sage working canvas, dark teal navigation, editorial rather than card-heavy construction choices, numeric fields with high-contrast labels, dark cost total, clean owner ranking bars. Decorative scene does not alter calculation inputs.

Assets: original procedural woven-cloth GLB created by scripts/create-cloth-model.mjs, original procedural weave texture in renderer, original static SVG fallback, self-hosted Archivo via Fontsource/SIL OFL. No third-party reference images reused.

Risk: Three.js adds download/render cost. Mitigation: lazy separate chunk, one scene at a time, capped pixel ratio, no shadows/post-processing/video, render paused offscreen, fallback always ready. Geometry model budget under 1 MB. APK uses same bundled web app.

Verification: desktop 1440 and phone 390 screenshots, controls and keyboard, reduced motion, blocked GLB fallback, calculator golden fixtures and browser sample, protected owner ledger. Code-led build; no approved image comp. Finish reviewer reviews captured evidence against this contract.
